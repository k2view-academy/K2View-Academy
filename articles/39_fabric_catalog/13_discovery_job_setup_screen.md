# Discovery Job Setup Screen

The Discovery job setup screen provides a full and comprehensive view of the Discovery job baseline configuration, including the list of product built-in plugins with their input parameters, sample size for the data snapshot step and more. 

In addition to viewing the baseline, this setup screen enables to perform the following updates:

* Override baseline configuration
* Add new plugins
* Create rules that will be applied during the job execution

